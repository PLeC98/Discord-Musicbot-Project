.env
cookies.txt
database/

# 실제 설정 — config/*.example.yaml에서 설치 때 복사된다(scripts/setup-config.js).
# 예시만 추적한다: 운영자가 고친 값이 PR에 실려 전역 기본값이 되지 않게.
config/genres.yaml
config/status.yaml
config/ai.yaml
config/ai-prompt.chatml
audio_cache/
node_modules/
dashboard/client/dist/
bgutil-ytdlp-pot-provider/
*.log
logs/
*.tmp
original_docs/
notes/
CLAUDE.md

# 내려받은 ffmpeg 바이너리 (scripts/install-ffmpeg.js)
bin/
